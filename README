auto installation script for macos
==================================

Requirements:
- macOS

==================================

Usage:

run `./setups.sh` first, to install brew and babashka,

then `./install.clj file.csv`

==================================

TODO:
 - skip already installed packages
 - add batch installation for brew, pip etc.
 - log failed packages to file for future processing
